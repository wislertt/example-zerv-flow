name: pr-unlock

on:
    pull_request:
        types: [closed, unlabeled]

permissions:
    contents: write

jobs:
    shared-pr-unlock:
        uses: wislertt/zerv/.github/workflows/shared-pr-unlock.yml@fa1f7946911a5194e1086b7445d16d75f90a6e4c # v0.8.32
        with:
            environments: '["d", "n", "p"]'
            deploy_label_prefix: "deploy-"
