name: pr-unlock

on:
    pull_request:
        types: [closed, unlabeled]

permissions:
    contents: write

jobs:
    shared-pr-unlock:
        uses: wislertt/zerv/.github/workflows/shared-pr-unlock.yml@7f7aedff6bd71a507e64bd806debae21c5dc162b # v0.8.34
        with:
            environments: '["d", "n", "p"]'
            deploy_label_prefix: "deploy-"
