name: pr-unlock

on:
    pull_request:
        types: [closed, unlabeled]

permissions:
    contents: write

jobs:
    shared-pr-unlock:
        uses: wislertt/zerv/.github/workflows/shared-pr-unlock.yml@2fcb93cf9b329a42ceff86dc96def74e8aa2e969 # v0.8.30
        with:
            environments: '["d", "n", "p"]'
            deploy_label_prefix: "deploy-"
